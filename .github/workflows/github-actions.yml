# Overview

name: github-actions 🚀

on: [push] # action
jobs:
  build: # job name
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: ls -l -a ${{ github.workspace }}
